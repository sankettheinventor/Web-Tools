import { defineConfig } from 'astro/config';

// Update `site` to your real domain when you have one — powers sitemap + canonical URLs.
export default defineConfig({
  site: 'https://snapjar.netlify.app',
});
