diff --git a/node_modules/@capacitor-community/admob/android/src/main/java/com/getcapacitor/community/admob/banner/BannerExecutor.java b/node_modules/@capacitor-community/admob/android/src/main/java/com/getcapacitor/community/admob/banner/BannerExecutor.java
index c629139..2748eeb 100644
--- a/node_modules/@capacitor-community/admob/android/src/main/java/com/getcapacitor/community/admob/banner/BannerExecutor.java
+++ b/node_modules/@capacitor-community/admob/android/src/main/java/com/getcapacitor/community/admob/banner/BannerExecutor.java
@@ -226,6 +226,11 @@ public class BannerExecutor extends Executor {
         activitySupplier
             .get()
             .runOnUiThread(() -> {
+                // SnapJar patch: guard against a race with removeBanner() (which async-nulls
+                // mAdView on the UI thread). Without this, a refresh/reposition that calls
+                // removeBanner()+showBanner() back-to-back could run loadAd on a null AdView →
+                // NullPointerException that crashes the whole app.
+                if (mAdView == null) return;
                 final AdRequest adRequest = RequestHelper.createRequest(adOptions);
                 mAdView.loadAd(adRequest);
             });
diff --git a/node_modules/@capacitor-community/admob/android/src/main/java/com/getcapacitor/community/admob/helpers/RequestHelper.java b/node_modules/@capacitor-community/admob/android/src/main/java/com/getcapacitor/community/admob/helpers/RequestHelper.java
index 8affa64..c43fb5a 100644
--- a/node_modules/@capacitor-community/admob/android/src/main/java/com/getcapacitor/community/admob/helpers/RequestHelper.java
+++ b/node_modules/@capacitor-community/admob/android/src/main/java/com/getcapacitor/community/admob/helpers/RequestHelper.java
@@ -24,6 +24,16 @@ public final class RequestHelper {
             adRequestBuilder.addNetworkExtrasBundle(AdMobAdapter.class, extras);
         }
 
+        // SnapJar patch: contextual targeting — finance keywords + content URL nudge the auction.
+        if (adOptions.keywords != null) {
+            for (String k : adOptions.keywords) {
+                if (k != null && !k.isEmpty()) adRequestBuilder.addKeyword(k);
+            }
+        }
+        if (adOptions.contentUrl != null && !adOptions.contentUrl.isEmpty()) {
+            adRequestBuilder.setContentUrl(adOptions.contentUrl);
+        }
+
         return adRequestBuilder.build();
     }
 
diff --git a/node_modules/@capacitor-community/admob/android/src/main/java/com/getcapacitor/community/admob/models/AdOptions.java b/node_modules/@capacitor-community/admob/android/src/main/java/com/getcapacitor/community/admob/models/AdOptions.java
index 9fdaf8b..67e3b9d 100644
--- a/node_modules/@capacitor-community/admob/android/src/main/java/com/getcapacitor/community/admob/models/AdOptions.java
+++ b/node_modules/@capacitor-community/admob/android/src/main/java/com/getcapacitor/community/admob/models/AdOptions.java
@@ -66,6 +66,10 @@ public abstract class AdOptions {
      */
     public final SsvInfo ssvInfo;
 
+    /** SnapJar patch: contextual targeting — finance keywords + content URL passed from JS. */
+    public final java.util.List<String> keywords;
+    public final String contentUrl;
+
     private AdOptions(PluginCall call) {
         /*
          * TODO: Since the Id in the Typescript AdOptions interface is not optional
@@ -81,6 +85,11 @@ public abstract class AdOptions {
         this.npa = call.getBoolean("npa", false);
         this.ssvInfo = new SsvInfo(call);
 
+        java.util.List<String> kw = new java.util.ArrayList<>();
+        try { com.getcapacitor.JSArray ks = call.getArray("keywords"); if (ks != null) { for (Object o : ks.toList()) if (o != null) kw.add(String.valueOf(o)); } } catch (Exception e) {}
+        this.keywords = kw;
+        this.contentUrl = call.getString("contentUrl", null);
+
         String sizeString = call.getString("adSize", BannerAdSizeEnum.ADAPTIVE_BANNER.name());
         this.adSize = AdOptions.adSizeStringToAdSizeEnum(sizeString);
     }
@@ -93,6 +102,8 @@ public abstract class AdOptions {
         this.npa = npa;
         this.adSize = adSize;
         this.ssvInfo = ssvInfo;
+        this.keywords = new java.util.ArrayList<>();
+        this.contentUrl = null;
     }
 
     /**
